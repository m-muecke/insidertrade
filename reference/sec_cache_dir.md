# Get the cache directory

Returns the path to the directory where downloaded SEC data files are
cached.

## Usage

``` r
sec_cache_dir()
```

## Value

A [`character()`](https://rdrr.io/r/base/character.html) string with the
path to the cache directory.

## See also

Other cache:
[`sec_cache_clear()`](https://m-muecke.github.io/insidertrade/reference/sec_cache_clear.md)
